%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:topics, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:topics, :new)

.tabs-content
  -form_for :topic, url(:topics, :create), :class => 'form-horizontal' do |f|
    =partial 'topics/form', :locals => { :f => f }
